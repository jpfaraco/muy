// Rendering has moved to DrawingLayer (SVG) to allow correct layer z-ordering.
// This component is kept as a placeholder in case canvas-specific features are needed later.
export function AnimationCanvas() {
  return null
}
